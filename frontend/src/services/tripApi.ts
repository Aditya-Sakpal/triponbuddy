/**
 * Trip API Service
 * Centralized API calls for trip operations
 */

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://localhost:8000";

export const tripApi = {
  // Add future trip-related API calls here
};
