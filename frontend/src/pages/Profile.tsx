import { HeroSection, ProfileCard } from "@/components/profile";

const Profile = () => {
    return (
      <>
          <HeroSection />
          <ProfileCard />
      </>
  );
};

export default Profile;