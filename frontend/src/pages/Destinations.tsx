import { HeroSection, FilterSection, DestList } from "@/components/destination";

const Destinations = () => {
    return (
      <>
          <HeroSection />
          <FilterSection />
          <DestList />
      </>
  );
};

export default Destinations;